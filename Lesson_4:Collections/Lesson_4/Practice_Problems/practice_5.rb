hash = { a: 'ant', b: 'bear' }
hash.shift

#What does shift do? How can we find out?
#Removes a key, value pair and returns it as an array
#Find out by checking the docs.
#In the above case, will return [:a, 'ant']